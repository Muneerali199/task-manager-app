{
  "framework": null,
  "builds": [
    { "src": "backend", "use": "@vercel/node", "config": { "outputDirectory": "backend" } }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/server.js" },
    { "src": "/(.*)", "dest": "backend/server.js" }
  ]
}